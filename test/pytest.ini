[pytest]
env =
    ICAT_URL=http://icat_payara_container:8080

    # from outside a container, for example when running locally in an IDE against the docker ICAT stack
    #ICAT_URL=https://localhost:18181

    ICAT_USERNAME=root
    ICAT_PASSWORD=pw
    ICAT_AUTHENTICATOR_NAME=simple
    ICAT_CHECK_CERT=False
    SSL_CERT_VERIFICATION=False
